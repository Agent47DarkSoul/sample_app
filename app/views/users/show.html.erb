<%= provide(:title, @user.name) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>

  <ul>
    <% @statuses.each do |status| %>
      <li><%= status.content %></li>
    <% end %>
  </ul>
</div>